# FileHub 开发总结

> 记录从项目启动到现在的完整过程：做了什么、怎么做的、踩过哪些坑。
> 最后更新：2026-09-20

---

## 一、项目概述

**FileHub** 是一个类 GitHub 的**自定义文件托管站**——上传的不是源码，而是任意类型的文件（图片、文档、视频、音频、压缩包、字体……），用「仓库」组织内容，支持星标、评论、搜索与下载统计。

| 项目 | 说明 |
|---|---|
| 位置 | `E:\AI\GML\filehub` |
| 技术栈 | FastAPI + SQLite + Jinja2 + GSAP 3.15.0 |
| 访问 | http://127.0.0.1:8000 |
| 测试账号 | `alice` / `secret123`（另有 100 个种子账号，密码统一 `demo1234`） |

**当前数据规模**：102 用户 · 205 仓库 · 659 文件 · 325 关注 · 664 星标 · 84 评论

---

## 二、技术架构

### 文件结构

```
filehub/
├── server/main.py          # 单文件后端：路由、认证、存储、等级、Markdown、迁移
├── static/
│   ├── js/app.js           # 全部前端逻辑（约 2400 行）
│   ├── css/style.css       # 全部样式（约 1500 行）
│   └── vendor/gsap/        # GSAP + 11 个插件（本地化，无需联网）
├── templates/              # 13 个 Jinja2 模板
├── seed.py                 # 演示数据生成脚本
├── filehub.db              # SQLite 数据库（自动生成）
├── uploads/                # 文件实体（UUID 重命名防路径穿越）
├── requirements.txt
├── start.bat / start.sh
└── README.md
```

### 关键设计

- **无 ORM**：直接用标准库 `sqlite3`，SQL 写在路由里，便于理解
- **自动迁移**：`migrate()` 在启动时用 `ALTER TABLE` 补齐新列，老数据库无需删库
- **服务端渲染**：Jinja2 出首屏，JS 只做交互增强（评论、上传、弹窗等）
- **GSAP 本地化**：所有插件从 npm 官方包复制到 `static/vendor/`，离线可用

---

## 三、功能清单

### 核心功能

| 功能 | 说明 |
|---|---|
| 账号体系 | 注册/登录/退出，PBKDF2-SHA256 加盐 12 万次迭代，HttpOnly Cookie |
| 仓库式分组 | 每用户多仓库，含名称、描述、README |
| 目录层级 | 文件名含 `路径/文件.ext` 即自动建文件夹，支持面包屑导航 |
| 任意文件类型 | 图片/视频/音频/PDF/文本在线预览，其余下载，单文件 ≤ 200 MB |
| 星标 | 一键点星，带粒子迸发动效 |
| 评论 | 发表/编辑/删除，悬停显示操作，仅作者可改删 |
| 搜索 | 按仓库名、描述、路径、标签、用户名检索 |
| 下载统计 | 单文件次数 + 仓库总下载量 |

### 深度功能

| 功能 | 说明 |
|---|---|
| 头像 | 拖拽裁剪（正方形，实时圆形预览）+ 6 个内置 SVG 头像 |
| 私有仓库 | 一键切换，对他人返回 **404**（不泄露存在性），首页/搜索/API/RSS 全过滤 |
| 整仓打包 | 一键导出 zip，保留文件夹结构 |
| 批量操作 | 全选/多选 + 一次性删除 + 二次确认 |
| 缩略图 | 图片上传自动生成，损坏图片优雅降级为类型图标 |
| 标签与备注 | 给文件打标签写备注，搜索结果中可见 |
| 文件夹别名 | 给文件夹起中文显示名，实际路径不变 |
| 在线编辑 README | 网页内写 Markdown 并保存，自动创建 README.md |
| RSS / JSON API | 每仓库 RSS feed；`/api/repos` 等接口 |
| 关注系统 | 关注/取关，粉丝与「正在关注」可点击查看名单并直接关注 |
| **等级系统** | Lv1–Lv10，按仓库数量递增，每级独立配色 |
| 个人页背景 | 上传自定义图片，可视化调节大小/位置/不透明度 |
| 可编辑简介 | 个人页简介支持在线编辑 |
| 社区聊天室 | 单一公共房间：引用回复、链接自动识别、60 个表情、时间精确到分钟 |
| **3D 方块矩阵背景** | 首屏背景由 canvas 绘制，方块成矩阵排列、随鼠标突起；材质与线条参考莱茵生命终端（见第四之二节） |

### 等级曲线

| 等级 | Lv1 | Lv2 | Lv3 | Lv4 | Lv5 | Lv6 | Lv7 | Lv8 | Lv9 | Lv10 |
|---|---|---|---|---|---|---|---|---|---|---|
| 所需仓库数 | 0 | 1 | 2 | 5 | 10 | 18 | 30 | 45 | 65 | **100** |

---

## 四、GSAP 动效实现

严格按照 GSAP 官方教程（读完了官网 9 篇文档）实现，不是随手加动画。

| 官方规范 | 本项目落地 |
|---|---|
| **FOUC 防闪**：CSS 先隐藏，用 `autoAlpha` 亮出 | 仅首屏标题预隐藏，其余内容默认可见（避免触发器失效导致内容永久不可见） |
| **`document.fonts.ready` 后再切分文字** | 所有 `SplitText.create()` 都在字体就绪后执行，避免中文换行错位 |
| **`mask` + `autoSplit` + `onSplit`** | Hero 标题逐字上推，窗口变化自动重切分 |
| **逐词滚动点亮（`scrub`）** | `.split-why` 文本随滚动逐词点亮，关键词标绿 |
| **`ScrollTrigger.batch` 批量入场** | 卡片、文件行错峰淡入上移 |
| **`gsap.matchMedia()` 尊重系统偏好** | `prefers-reduced-motion` 时走静态降级 |
| **站内动效开关** | 设置面板内的开关，写 `localStorage`，实时重建上下文 |
| **`ScrollSmoother` 平滑滚动** | `data-speed`/`data-lag` 做视差；**固定元素必须放在 wrapper 外** |
| **`Flip` 位移动画** | 文件列表排序切换 |
| **`gsap.quickTo()` 高频跟手** | 副标题磁性跟随、鼠标跟随效果 |
| **Apple 式跑马灯** | 圆点生长→展开→内容浮现的入场；播放/暂停 + 圆点分页 |

**性能原则**：只动 `transform` / `opacity`；UI 过渡统一 `power*.out`。

### 泛光配色：跟随强调色，但去饱和

站点有「强调色」设置，但**光晕和淡色底纹不应该跟着变鲜艳**——否则紫按钮配紫光、绿按钮配绿光，整页都在喊同一个色相。做法是引入三个由强调色推导、饱和度大幅降低的变量：

```css
--glow:        color-mix(in srgb, var(--accent) 30%, var(--muted));  /* 92% → 18% */
--glow-strong: color-mix(in srgb, var(--accent) 45%, var(--muted));  /* 92% → 32% */
```

`color-mix()` 把强调色混进中性灰里，得到的是「带一点色调的灰」，而不是「压低亮度的彩色」。强调色本身仍然负责文字、图标和主色块——**只有它投出的光被调暗了**，两者才不会互相打架。

覆盖范围：所有 `box-shadow` 光晕、标签/提示条的淡底、仓库头部与上传区的底纹、热力图整条色阶。而`.ptr-*` 鼠标跟随层那几个几何图形**故意保留**纯强调色——它们是图形本身，不是泛光。

---

## 四之二、交互细节

| 交互 | 做法 |
|---|---|
| **磁性按钮** | 感应区从**按钮边缘**算起 **10px**（不是从中心），并在区内线性衰减。从中心算半径会让宽按钮甩出几十像素的力场；从边缘算则每个按钮手感一致。实测：边缘外 9px 位移 2.2px → 6px 处 6.9px → 贴边 14.5px，平滑无跳变 |
| **热力图提示框** | 弃用原生 `title`（浏览器自绘、样式不可控、触屏不显示），改为站点自己的浮层：悬停 **0.2 秒**后出现，只在**有活动**的格子上显示，格间移动时不重复等待。挂在 `<body>` 上（卡片会裁切溢出），用 `position: fixed` 并对边缘做收边。**命中判定按坐标、不按 `e.target`**（见下） |
| **等级面板** | 高度 162 → 107px：它是一个状态条，不该占掉首屏三分之一（收紧内边距、缩小 Lv 大字与刻度点） |
| **评论头像** | 26 → 40px。这个尺寸是给导航栏和用户列表调的（头像只是名字旁的小标识），但评论里头像是整行的锚点，26px 配正文太小 |
| **仓库命名** | 放开到汉字/字母/数字/常用符号，见下 |

> **一条划错的边界**：我一度把热力图整条色阶也"去饱和"了，结果网格变成一片灰紫、看不出活动强度——**数据和泛光是两回事**。色阶是数据，保留自己的绿色层次；只把 lv4 那圈外发光调柔和。改配色时要分清「信息」和「氛围」。

### 仓库命名的放开与红线

原来只允许 `[A-Za-z0-9_.-]`，中文完全不能用。现在改成「白名单式排除」：

- **放行**：汉字、日文、韩文、拉丁字母、数字、以及常见符号（`（）`、`【】`、`·` 等）。用 Unicode 字符类实现，所以不需要逐个语言列举。
- **仍然拒绝**：`/ \ ? # % < > " ' \` | : *` 和控制字符——仓库名要作为 URL 路径段，编码成 `%E4%B8%AD` 会让所有链接和面包屑失去可读性；点号开头/结尾（`.` 和 `..` 对文件系统另有含义）；超长（64 字符）。
- **关键词黑名单**：`hidden`、`class`、`none`、`important`、`inherit`、`repos`、`api`、`con` 等约 90 个词。它们不是语法错误，但仓库名会变成元素 id、CSS 变量后缀、data 属性值散落在模板里，其中与真实关键字重名的（`hidden`、`none`、`important`）会**静默破坏渲染它的那个页面**。比较时做 casefold，所以 `Hidden` 也被拦。

---

## 四之三、Hero 背景：3D 方块矩阵（canvas，非 GSAP）

`.hero-bg` 里原来放的是三个模糊光斑 + 一张 CSS 网格，现已换成一整块 canvas 绘制的**立体方块矩阵**：方块排成矩阵、间隙很小，鼠标移过去会像积木一样突起，方块颜色跟随设置面板里的强调色。

### 为什么用 canvas 而不是 DOM

矩阵有 400~600 个方块。用 DOM 的话每个方块都要带自己的 `transform`，每帧都是一次样式重算；canvas 只是一层合成层。

### 投影是真的（不是斜切）

相机悬浮在方块平面上方 3000 单位、俯角 60°，方块的四角各自做针孔投影：`屏幕坐标 = 与相机轴的偏移 / 深度`，那个除法就是收敛感的来源（越远越小、并向消失点收拢）。

- **方块要在屏幕上是正方形**：在这个俯角下，进深方向 1 个世界单位会落到 `1/sin(60°)` 个屏幕像素上——进深是被**拉伸**而不是压扁的——所以进深取 `宽度 × sin(60°)`。实测 62px 宽的方块，屏幕上正好 62px 高。
- **铺满整幅画面**：投影随深度变窄，顶部两角需要更宽的世界范围才能盖住。宽度取最远那一行所需的宽度，整幅统一使用，列就始终对齐。
- **抬升量用屏幕像素、不用世界高度**（关键教训）：从这么高的位置俯视时高度轴被压得几乎没有——1 个世界单位的高度只值约 0.1 个屏幕像素——用固定世界高度会出现「近处几乎不动、远处才动一点」。改成固定屏幕像素后，无论远近、突起幅度都一致。
- 突起时顶面会略微放大（`FLARE`），模拟升高后离相机更近的那点透视；加上深色的侧面，方块才有体积感。

### 方块的面，借的是莱茵生命终端的语言

配色与材质参考 [RhineLabUI](https://github.com/LBEILC/RhineLabUI)（《明日方舟》莱茵生命：访问 终端界面的复刻），但只借它的**视觉语言**：形状仍是正方形、视角仍是 60° 俯视。

| 参考里的做法 | 这里的落地 |
|---|---|
| 透明/磨砂档案盒 | 方块不是实心色块，而是**极淡的白色半透明面板**（`rgba(224,227,220, α)`，α 只有 0.022~0.055） |
| 卡片顶面受光 | 面板上缘一条**受光带**（两级台阶做渐变，见下）+ 最上方一条**发丝线** |
| `细线与紧凑排字` | 方块之间的分界线不画边框，是**缝隙里透出的底色**；只有最上缘那条线是画出来的 |
| 磨砂 → 清晰（解密时玻璃变透） | 方块一被抬起就**停止磨砂**：面转成强调色、边缘提亮到近白 |
| 呼吸：8 秒与 13 秒周期叠加 | 静止起伏用同样两个周期叠加，外加每块一个相位偏移，所以是流动而不是整体脉动 |
| 深色主题 `#11181b` / 分隔线 `#536166` | 面板底色沿用页面的深色底，发丝线用 `#e0e3dc` 的低透明度版本 |

两点实现取舍：

- **受光带用两级台阶而不是真渐变**：每块每帧新建一个 `LinearGradient` 的开销远大于多一次填充，而这个尺寸下眼睛会把两级台阶读成一条柔和过渡。
- **发丝线按设备像素算厚度**（`EDGE_PX / DPR`），否则在 Retina 屏上会变成 2px 的边框，就不是「细线」了。

### 与站点其他部分的关系

- 跟随系统的 `prefers-reduced-motion`，也跟随设置面板的「动效」开关：关掉时只画一帧静态画面（背景仍然在，不是空白）。
- 标签页隐藏、或 hero 滚出视口时自动暂停 `requestAnimationFrame`，和 `registerAmbient` 的取舍一致。
- `.hero-bg` 是 `pointer-events: none`，所以方块不会挡住按钮点击；文字层 `z-index: 2` 在它之上。
- 强调色每帧从 `window.__fhAccent` 读，设置面板换色立即生效，不需要重建。

### 可调参数（`static/js/app.js` 顶部）

| 参数 | 作用 | 当前值 |
|---|---|---|
| `TILE_PITCH` / `TILE_GAP` | 方块间距 / 缝隙 | 68 / 6 |
| `CAM_PITCH` / `CAM_H` | 俯角 / 相机高度（越大透视越平） | 60° / 3000 |
| `LIFT_PX` / `IDLE_PX` | 突起高度 / 静止起伏 | 42 / 6 |
| `LIFT_REACH` | 鼠标影响半径（近端像素） | 165 |
| `FLARE` | 突起时的放大比例 | 0.06 |
| `FACE_*` / `SHEEN_*` / `CORE_*` / `EDGE_*` | 面板 / 受光带 / 亮芯 / 发丝线各自的颜色与透明度，`_FAR` 是远端、`_NEAR` 是近端 | 见文件 |
| `SHEEN_BAND` / `CORE_BAND` | 受光带、亮芯占方块高度的比例 | 0.32 / 0.13 |
| `EDGE_PX` | 发丝线厚度（**设备像素**） | 1.5 |
| `ACCENT_TOP` / `ACCENT_SIDE` | 抬起后顶面 / 侧面的不透明度 | 0.8 / 0.3 |

> 想要更接近参考站那种「几乎看不见的线」，把 `EDGE_NEAR` / `SHEEN_NEAR` 的 α 往下压即可；反之最亮处就是这几个值。

### 首页胶囊的两种入场动画

参考 Apple 官网 iPhone 页的 media-card gallery。**参数直接取自 Apple 自己的样式表**，不是目测的——它把时延声明成了 CSS 自定义属性，读出来就行：

```css
/* .aap-media-card-gallery */
--aap-background-transition-duration: 250ms
--dotnav-opacity-delay:        740ms   (+100ms 淡入)
--playpause-opacity-delay:     940ms   (+100ms 淡入)
--playpause-scale-delay:       940ms   (+200ms 缩放)
```

设置面板可二选一：

| 风格 | 编排 |
|---|---|
| **从中心展开** | 一颗种子弹出 → 胶囊从 56px 长到全宽（1.15s，`power3.inOut`）→ 播放按钮与圆点分页在尾部同时归位 |
| **Apple 式分段浮现**（默认） | **严格串行的两幕**：A 幕 0–477ms，胶囊保持 56px 窄条、从屏幕外（实测起点 `rectTop 957` > 视口 900）弹入到位（`power3.out`，快速弹出后持续减速）；B 幕 477–858ms，到位后才从中间向两侧展开到 1180px（`back.out(1.4)` 轻微过冲）。淡入 `power1.out` 跨越整个上升过程。随后控件按 Apple 的 740/940ms 登场，整体约 1.5s 收尾 |

**这里踩过一个典型的编排坑，值得记**：最初把"宽度展开"和"位移动画"都安排在 `t=0` 并行——结果出现「先往下窜、再弹回来」的错乱观感。原因是**宽度变化作用在居中 flex 元素上时是以中心为基准向两侧生长**，它与纵向位移叠加后互相干扰。修法不是调缓动，而是**把两件事拆成前后串行**：先纯粹地弹入（宽度不动），到位后再展开。判断标准很清楚——实测 `t=477ms` 时同时满足 `top=0` 且 `w=56`，说明两个动作彻底分离了。

### 位移属性连踩三坑：`y` → `marginTop` → `top`

这是本次最难查的一处，**同一个"胶囊像在原地"的现象，三种属性各有各的失败原因**，值得完整记下来：

| 尝试 | 现象 | 原因 |
|---|---|---|
| `y` / `translateY` | 内联写 `translate(0px, 85.8px)`，计算值却是 `matrix(1,0,0,1,0,13.9)`；更诡异的是某些帧两者**方向相反**（内联 85→27→5 时计算值 13.9→21.7→20.3） | 胶囊位于 `#smooth-content` 内——那是 `ScrollSmoother` 的包装容器，**每帧重写自己的 transform** 来实现平滑滚动。GSAP 写在后代上的 transform 不是最终被绘制的那一个。加 `force3D: false` 无效 |
| `marginTop` | 148px 的外边距只让胶囊移动了 74px | margin 在**居中的 flex 列**里会被"重新居中"吸收掉一半。补一个 `padding-bottom` 预留空间后，预留本身又改变了行的静止位置，让距离计算变成了循环依赖 |
| `top`（配合 `position: relative`） | ✅ 有效 | 它是**绘制期偏移、不参与布局流**，所以写多少就走多少，也不牵动 hero 里的其他元素。实测 `top: 205 → 0` 对应 `rectTop: 957 → 752`，一比一 |

**教训**：在带 `ScrollSmoother`（或任何逐帧重写 transform 的容器）的页面里做**大幅位移**，优先用布局/绘制期属性（`top`、`margin`），而不是 `transform`。若要查这类问题，**别只看内联 style**——要同时读 `getComputedStyle().transform`，两者不一致就说明 transform 管线被别的东西接管了。

另外，`--pill-rise` 那套 padding 预留已随方案废弃并清理，避免留下死代码。

实测各控件登场时刻与 Apple 规格的对照：

| 元素 | 本项目实测 | Apple 声明 |
|---|---|---|
| 圆点分页 | 766ms | 740ms |
| 播放按钮 | **941ms** | **940ms** |

**排查这个动画时踩的坑（值得记）**：我用 `element.scrollIntoView()` 和 `window.scrollTo()` 反复采样，**什么变化都采不到**——因为 Apple 的动画是**逐帧读取滚动量**算出来的，瞬移滚动一帧跳到位，中间过程被完全跳过，它只看到起点和终点。**用户用真实滚轮滑动时动画是正常的**。要观测这类滚动驱动动画，必须用连续、带惯性的真实滚轮事件，而不是编程式跳转。

另外它的 `__intro-element` 的 `opacity: 0` 是**永久终态**（动画播完后归零隐藏），不是动画中间态——我一度把它当成"还没播"的证据，多花了不少时间。

**顺带修的隐患**：胶囊在视口外时会被置为 `entering`（钉在 56px 窄条）。如果 `ScrollTrigger` 因为任何原因没触发（视口塌陷、设置重建时被 kill、refresh 错过边界），它就会**永久停成一根窄条、控件不可见**。加了两道兜底：4 秒死线开关自动落到最终态，以及时间轴的 `onInterrupt` 回调。

> 开关是**一次性**的：两种入场都在页面加载时触发，所以切换风格会提示"刷新后生效"，而不是假装立刻能重播。

---

## 五、修复过的关键 Bug

### GSAP 相关

1. **`mm.killAll()` 不存在** → GSAP 3.15 中应为 `mm.kill()`
2. **暂时性死区（踩了 3 次）**：`const` 声明在调用之后 → 把所有初始化收拢到文件末尾的 `boot` 区块
3. **脚本加载时序**：`<script>` 在设置面板 DOM 之前执行，导致 `initSettingsPanel()` 直接 return → 脚本移到 `</body>` 前
4. **`display:flex` 覆盖 `hidden` 属性（踩了 4 次）**：预览弹窗、仓库编辑器、bulk-bar 都中了 → 加全局规则 `[hidden]:not(.gsap-fade) { display: none !important }`
5. **`height:0` 不包含 padding/border** → bulk-bar 收起时需同时把 padding 和 border 归零
6. **GSAP 残留内联样式覆盖 `hidden`** → 动画结束用 `clearProps` 清理

### 架构相关

7. **ScrollSmoother 的 transform 让 `position:fixed` 失效**：弹窗相对祖先而非视口定位，导致偏移 → 所有固定层重挂到 `<body>`
8. **登录/注册无限重定向**：关闭时用 `document.referrer` 回溯，两页互为 referrer 形成死循环 → 统一回首页
9. **Windows 上 unlink 打开中的文件抛异常**：掩盖了真实的「文件过大」错误，返回 500 而非 400 → 先关闭句柄再删

### 数据与权限

10. **私有仓库对未登录返回 404 是正确行为**——曾误判为 bug，实际是设计
11. **公共聊天室无法发消息**：用 `repo_id=0` 但 `comments.repo_id` 有外键约束 → 重建表移除该约束（79 条历史评论完整保留）

### 性能

12. **拖拽窗口时掉帧**：根源是三个色块的 `filter: blur(90px)`，浏览器每帧重算大面积模糊 → 改用**径向渐变**（视觉等价，零 GPU 开销）+ 标签页不可见时暂停动画

### 内容可见性

13. **内容永久不可见（最严重）**：`.js-card` 用 CSS 预隐藏，但 `ScrollTrigger.batch` 的 `once:true` 对「已在视口内」的元素不触发 `onEnter`（官方文档中的典型误区）→ 改为内容默认可见 + JS 侧兜底显示

### 静态资源与路由

14. **「自定义背景」上传后 404（功能从未可用）**：模板里的表单指向 `/settings/banner/upload`，但**这条路由根本没写过**；`/settings/banner/remove` 和 `/media/banner/{name}` 同样缺失（avatar / cover / thumb 都有读取路由，只有 banner 漏了）。所以 `save_banner` 能存 scale/x/y/opacity，`image` 却永远是空的——图片根本没地方存。→ 补齐三条路由。顺手做了安全验证：路径穿越（`../../filehub.db`、URL 编码变体）被文件名的正则白名单挡在 404，非图片文件被 `make_banner` 识别失败后返回 400。
15. **改了 CSS/JS 但页面不更新（踩了很久）**：模板里写的是 `style.css?v=92` 这种**硬编码**版本号，改了静态文件忘了提升版本号，浏览器就一直吃缓存。→ 改成服务端按文件 **mtime + 体积**算出 token 注入模板（`asset_v`），任何改动自动失效，不再依赖人记得改数字。**排查这个坑的教训**：先用 `curl` 对比「服务端发出的内容」和「磁盘上的内容」，两者一致就说明问题在浏览器缓存，而不是文件没改成功。
16. **热力图色阶断裂**：四个色阶是硬编码的绿色，但 `lv4` 单独被 `body.has-accent` 改成强调色 → 选了紫色强调色后，lv1~3 还是绿、只有 lv4 变紫，色阶读起来是坏的，而且那个紫块非常扎眼。→ 整条色阶改由强调色推导，任何强调色下都连贯。
17. **热力图提示框一闪而过（三层 bug 叠在一起）**：表面现象是"提示框闪一下就没了"，实际是三个独立问题，每一个单独都能造成这个现象：
    - **用 `e.target` 判定格子**：格子之间有 4px 间隙，指针扫过间隙时 `closest(".hm-day")` 返回 `null` → 立刻 `hideTip()`。38px 的格子上横向移动几乎必然踩到间隙。→ 改为**按坐标做几何命中**。
    - **滚动时无条件隐藏**：`ScrollSmoother` 用 transform 做平滑，一次滚轮会持续几百毫秒不断触发 `scroll` 事件，每次都 `hideTip()`。→ 滚动只**刷新缓存坐标并重新定位**，不再隐藏。tooltip 是 `position: fixed`，所以必须重定位，否则会留在原地、和格子错开。
    - **GSAP 淡出回调竞态**：`hide` 的 `onComplete` 可能晚于下一次 `show` 执行，把刚显示的又藏起来。→ 用独立的 `tipShowing` 标志，不再依赖 `hidden` 状态判断。
18. **（修正 #17 的过度修复）空格子该清除却保留了旧信息**：修 #17 时我把"间隙"和"空格子"当成了同一件事——为了不让间隙闪烁，写成了"移到没有活动的格子时保持显示不动"。结果：先停在活动格（显示"9 月 8 日 · 5 个文件"），再移到旁边的空格子，**上一条信息赖着不走**。
    根因是**两种状态被混为一谈**。正确的模型是三态，而不是"有/无"两态：

    | 指针位置 | 含义 | 动作 |
    |---|---|---|
    | 格子外（热力图矩形之外） | 离开了 | 隐藏 |
    | 格子**之间**的几像素间隙 | 正在路过 | **保持不动**（这才是防闪烁的那一条） |
    | 某个格子 | 落到某天 | 有活动→显示；没活动→**隐藏** |

    `cellAt()` 现在返回 `元素 / undefined（间隙）/ null（格子外）` 三种值，调用方分别处理。**教训**：当一个"防抖"式的修复引入了新行为时，先问清楚被合并的是哪几种语义——把它们当成一种，就会把其中一种的正确行为一起改掉。

19. **（测试方法的坑，踩了两次）用过期坐标做验证**：我一度测出"活动格→空格子仍显示旧信息"，据此以为 #18 没修好。实际是**页面滚动后我还在用滚动前的坐标**，鼠标根本没落在空格子上。同样地，`element.scrollIntoView()` 在有 `ScrollSmoother` 的页面上不可靠（滚动异步且带惯性），读到的 rect 是滚动前的旧值。
    正确做法：用 `ScrollSmoother.get().scrollTop(...)` 滚动，**等惯性完全停下**再测量，并且**每次动作前重新读一次坐标**而不是复用缓存。另外，验证悬停类交互时每次停留要**超过 `HOVER_DELAY`**，用 40ms 一次的高频采样会把计时器反复重置，测出来的全是"没反应"。

---

## 六、协作经验

### 我犯过并改正的错误

- **误删代码**：删除滑动高亮时，连带删掉了 `start` 函数和 `.marquee-track` 的 CSS 规则，导致滚动停摆和标签错位 26px（表现为"重影"）
- **模板编辑失误**：修改个人页时索引计算错误，误删了热力图和简介两个区块，事后重建
- **布局反复试错 3 次**：`space-between` 和 `margin-top` 都会把内容块与胶囊**拆散**，正确做法是把两者当作**一组**居中（`justify-content: center` + `gap`）

### 验证方法论的教训

- **不要只靠截图判断布局**：浏览器截图工具在滚动/动画期间会产生**残影**（多次误导我以为是真实渲染问题）
- **用 DOM 数据验证**：例如判断三组标签是否对齐，看 `setTops: [870, 870, 870]` 比看截图可靠得多
- **每次改模板后立即验证渲染**，不只看代码是否正确

---

## 七、验收状态

- **23 项路由/权限断言全部通过**
- **服务端零 Traceback**
- **浏览器逐项截图确认**：评论编辑、头像上传、封面切换、简介保存、等级面板、跑马灯、弹窗居中、热力图等

---

## 七之二、页面与数据的一些约定

### 卡片布局：固定 4 列

仓库卡片在**个人页、仓库列表、搜索页**三处统一为 4 列（原来 `auto-fill + minmax(300px)` 在 1200px 内容宽度下只放得下 3 列）。卡片保持**横排**——封面在左、文字在右，封面从 110px 收到 84px 以适配 287px 的卡宽；断点依次降到 3 / 2 / 1 列。

### 三个列表页的分工

| 页面 | 内容 |
|---|---|
| **`/repos`** | 用户区（**每次刷新随机 12 位**，站长置顶）+ 原有的全部仓库网格 |
| **`/search`（空查询）** | 三个**切换按钮**（最多星标 / 最近更新 / 下载最多）控制**同一块网格** + 随机 50 位用户 + 「全部用户」入口 |
| **`/people`** | 全部用户，**6 列**，6 种排序（最新/最早加入、名称正序/倒序、粉丝最多、仓库最多） |

几点设计取舍：

- **随机而不是固定排序**：用户列表按固定顺序排，每次刷新看到的是同一批人。改成 `ORDER BY RANDOM()` 后每刷新一次都是新面孔（`/repos` 12 位、`/search` 50 位），目录页才有"活着"的感觉。这个规模下一次全表扫描没有索引可破坏，性能不是问题。
- **站长置顶**：随机意味着站长有 11/12 的概率不出现在列表里。所以随机时**排除站长再手动前置**，而不是靠运气——建站的人不该在列表里时隐时现。
- **三个分组改成切换按钮**：三组各显示 8 个仓库时，**同一个仓库经常同时出现在两组里**（星标多的通常也更新得勤），三块网格堆在一页既冗长又重复。改成一个按钮控制一块网格后，同一时间只显示一组，并且每组从 8 个提到 24 个。
- **删除"全部仓库"整块**：搜索框下面挂一份全站索引，既不是搜索也不是浏览。空查询现在给的是三组精选 + 用户，有明确取舍。

用户卡片用独立的 `people-grid` / `person-card` 样式（与仓库卡一样的列节奏，但以头像为主，因为这一列表讲的是"人"而不是"上传了什么"）。

### 页面背景（个人页 + 社区页）

两处背景是**同一套机制**：`position: absolute; inset: 0` 的外层 + 内层做 `scale` / `background-position` / `filter`，配置存成 JSON 存在用户行上（`users.banner` / `users.chat_bg`），四个路由共用同一份实现（`_upload_scene_image` / `_remove_scene_image` / `_save_scene`）。

**踩过的三个坑**：

1. **`background-size` 必须是百分比，不能是默认的 `auto`**。`auto` 按原图尺寸绘制，当图片小于容器时 `background-position` 的百分比**没有可移动余量**——0% 到 100% 全落在同一位置，滑动条看起来完全失效。改成 `170%` 后垂直可移动范围从 **0px** 变成 **587px**（缩放 100% 时）/ **1381px**（缩放 200% 时）。
2. **个人页的背景会跟着"自定义背景"按钮一起动**：面板原本在 `.repo-hero` **内部**，展开时把 hero 撑高，而背景是相对 hero 定位的 `inset: 0`，于是被拉伸。把面板移到 `</section>` 之后就好了。保存时那次缩放脉冲也一并去掉——它动的正是用户刚调好的东西。
3. **设置抽屉里的间距不够**：那套控件原本是给 620px 的个人页面板设计的，放进 380px 的抽屉后，52px 标签 + 44px 数值只给滑杆留下约 200px。改成**标签与数值一行、滑杆独占下一行**后，滑杆可用宽度回到 325px，行高从 26px 提到 38px。


### 站长标识

`users` 表**没有角色字段**，也没有必要为这一个标记加表和迁移：**id 最小的那个账号**就是建站的人，也就是要显示"站长"的人。服务端把它缓存在进程里（`site_owner_name()`，每页渲染都要读），通过 `page_ctx` 注入所有模板。

两处显示，尺寸不同：搜索页的用户卡片跟在小字名字后（7.9px），个人页跟在一个 **46px 的大标题**后（7.4px，字号已压到可读下限）。**个人页那个必须放在 `h1` 外面**——`SplitText` 会把 `h1` 拆成逐字 span，塞进去的标识会被当成字母一起切碎。

### 返回时不留旧数据

浏览器有**两种**机制让"返回"显示过期的页面，需要分别处理：

| 机制 | 现象 | 处理 |
|---|---|---|
| 滚动恢复 | 返回后停在离开时的位置，内容却是新的 | `history.scrollRestoration = "manual"`，另需把 `ScrollSmoother` 自己的偏移归零（它不跟随原生滚动位置） |
| back/forward cache | 返回的是**旧 DOM**，填过的表单、展开的面板都还在 | `pageshow` 的 `persisted: true` 是它被启用的信号，此时 `location.reload()`；服务端同时给所有 HTML 响应加 `Cache-Control: no-store` |

**注意**：`no-store` 单独并不足以阻止 bfcache——Chromium 仍会把这类页面放进去。所以两边都要做。

### 一处漏改导致的 bug

放开中文仓库名时，我改了新建表单却**漏了编辑表单**：`repo.html` 里的 `pattern` 还是 `[A-Za-z0-9][A-Za-z0-9_.-]{0,63}`。结果是"新建能用中文名，改名时被浏览器拦下"。**教训**：同一个校验规则出现在几处表单里时，改一处就要全局搜一遍 `pattern=` / 同样的正则。

---

## 八、待办与可调项

| 项目 | 位置 | 当前值 |
|---|---|---|
| 封面存储尺寸 | `server/main.py` `COVER_SIZE` | 720×720 |
| 封面显示尺寸 | `style.css` `.repo-cover-square` | 240px（hero）/ 110px（卡片） |
| 等级曲线 | `server/main.py` `LEVEL_THRESHOLDS` | `[0,1,2,5,10,18,30,45,65,100]` |
| 单文件上限 | `server/main.py` `MAX_FILE_SIZE` | 200 MB |
| 封面/背景上限 | `server/main.py` `MAX_COVER_SIZE` | 10 MB |
| 界面缩放 | 设置面板 | 60%–100%，默认 80% |
| 跑马灯标签 | `templates/index.html` | 24 个类型 |
| 测试数据 | 数据库 | alice 名下有测试封面/简介/评论，可清理 |

### 已知边界

- 上传**未做分片续传**，超大文件中途断网需重传
- Markdown 渲染是轻量实现，不追求完整 CommonMark
- 视频缩略图未实现（需 ffmpeg 依赖，目前降级为类型图标）
- 部署到公网建议：换 PostgreSQL、会话移入 Redis、补 HTTPS 与 CSRF 防护
